package com.sr.capital.repository;

import com.sr.capital.entity.BaseCreditPartnerCredEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BaseCreditPartnerCredRepository extends JpaRepository<BaseCreditPartnerCredEntity,Long> {
}
