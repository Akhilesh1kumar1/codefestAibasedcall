threads: ${REDIS_MAX_THREADS}
nettyThreads: ${REDIS_NETTY_THREADS}
singleServerConfig:
  address: ${REDIS_URL}
  connectionMinimumIdleSize: ${REDIS_MINIMUM_SIZE}
  connectionPoolSize: ${REDIS_CONNECTION_POOL_SIZE}
  #password: ${REDIS_PASSWORD}